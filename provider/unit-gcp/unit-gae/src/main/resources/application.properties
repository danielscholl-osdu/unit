LOG_PREFIX=unit

server.servlet.contextPath=/api/unit/
logging.level.org.springframework.web=DEBUG
server.port=8080

spring.main.allow-bean-definition-overriding=true

# Entitlement properties
#
#
# [Required] URL of the Entitlements Service
osdu.entitlement.url=${ENTITLEMENT_URL}

# Other properties
#
#
# [Optional] [GAE] Specifies the name of the bucket which contains unit catalog
osdu.unit.gae.catalog.bucket=${UNIT_CATALOG_BUCKET:}
# [Optional] [GAE] Unit catalog file name in the bucket
osdu.unit.gae.catalog.file=${UNIT_CATALOG_FILENAME:UnitCatalog_V2.json}

google.audiences=123.apps.googleusercontent.com
partition.api=http://localhost:8081/api/partition/v1
